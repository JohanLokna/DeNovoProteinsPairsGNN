class TestProteinDesign:

    def __init__(
        self,
        dm,
        levels,
        repeats
    ) -> None:
        self.dm = dm
        self.levels = levels
        self.repeats = repeats

    def test(model) -> None:

        for level in self.levels:
            for _ in range(self.repeats):
                for x in 
